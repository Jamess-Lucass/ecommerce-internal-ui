apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecommerce-internal-ui-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: internal.jameslucas.uk
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ecommerce-internal-ui-service
                port:
                  number: 80
